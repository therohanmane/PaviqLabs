# Backend origin only (no /api suffix). Production example:
# VITE_API_URL=https://paviqlabs.onrender.com
#
# Local dev: point at your machine or leave unset to run without API (graceful empty state).
# VITE_API_URL=http://localhost:5000

VITE_API_URL=
